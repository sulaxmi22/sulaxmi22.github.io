version: '3.8'
services:
  backend:
    build: .
    ports: ["8002:8002"]
    env_file: [.env]
  frontend:
    image: nginx:alpine
    ports: ["3002:80"]
    volumes: ["./frontend:/usr/share/nginx/html:ro"]
    depends_on: [backend]
